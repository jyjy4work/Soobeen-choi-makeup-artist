{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /c/Users/parkj/Desktop/Soobeen/makeup-artist && git add . && git diff --cached --quiet || git commit -m 'auto: claude code update' && git push 2>/dev/null || true",
            "async": true
          }
        ]
      }
    ]
  }
}
